---
name: deployment
description: Use to plan CI/CD, Vercel or cloud deployment, observability, rollback and production gates without deploying automatically.
---
# Deployment
Inspect existing provider first. Validate build, environment schema, migrations, backup and health check. Production/staging external actions require HITL. Vercel is an option, not a default.
