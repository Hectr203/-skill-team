---
name: react
description: Use for React and TypeScript frontend implementation, state, accessibility and performance.
---
# React
Apply `skills/engineering/ponytail/` explicitly. Prefer semantic HTML, native browser APIs and existing dependencies. Keep API calls in services, UI declarative, domains isolated, and `prefers-reduced-motion` respected. Validate typecheck, lint, keyboard use and responsive states.
